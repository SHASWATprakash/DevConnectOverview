// src/services/endpoints.js
const endpoints = {
  auth: {
    login: "/auth/login",
    signup: "/auth/signup",
  },
  // Add other domains like user, project, comment here
};

export default endpoints;
